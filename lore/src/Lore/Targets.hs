module Lore.Targets
  ( LoadTargetsOptions (..),
    defaultLoadTargetsOptions,
    loadTargets,
    retainUnresolvedRollback,
  )
where

import Lore.Internal.Targets
  ( LoadTargetsOptions (..),
    defaultLoadTargetsOptions,
    loadTargets,
    retainUnresolvedRollback,
  )
