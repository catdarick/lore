module Lore.Targets
  ( LoadTargetsResult (..),
    LoadTargetsOptions (..),
    defaultLoadTargetsOptions,
    loadTargets,
    retainUnresolvedRollback,
  )
where

import Lore.Internal.Targets
  ( LoadTargetsOptions (..),
    LoadTargetsResult (..),
    defaultLoadTargetsOptions,
    loadTargets,
    retainUnresolvedRollback,
  )
