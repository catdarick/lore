name: lore-bench-small
version: 0.1.0.0

dependencies:
- base >= 4.7 && < 5
- containers
- text

default-extensions:
- TypeFamilies
- KindSignatures
- OverloadedRecordDot

library:
  source-dirs: src
